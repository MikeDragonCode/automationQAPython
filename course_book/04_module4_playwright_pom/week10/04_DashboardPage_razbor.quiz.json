{
  "questions": [
    {
      "question": "Почему в DashboardPage нет метода open_page(), в отличие от RegistrationPage?",
      "options": [
        "Потому что открытие страницы по URL в Playwright вообще невозможно",
        "Потому что метод open() отсутствует в самом BasePage",
        "Потому что в сценарии курса пользователь никогда не открывает Dashboard напрямую по URL — он попадает туда только после регистрации",
        "Потому что DashboardPage не наследуется от BasePage"
      ],
      "correct": 2,
      "explain": "Page Object должен содержать только то, что реально используется в сценариях: раз Dashboard в курсе открывается только через переход после регистрации, отдельный метод open_page() ему не нужен."
    },
    {
      "question": "Какой метод из BasePage переиспользует check_opened() в DashboardPage?",
      "options": [
        "__init__()",
        "check_current_url()",
        "goto()",
        "open()"
      ],
      "correct": 1,
      "explain": "check_opened() вызывает self.check_current_url(self.URL) — готовую проверку из BasePage, не дублируя логику проверки URL заново."
    },
    {
      "question": "Почему проверки check_opened() и check_visible_toolbar_title() разделены на два отдельных метода, а не объединены в один check_dashboard_page()?",
      "options": [
        "Разделение отражает два разных смысла: переход на нужную страницу и содержимое страницы, что упрощает диагностику при падении теста",
        "pytest требует минимум два метода проверки на каждый Page Object",
        "Это ускоряет выполнение проверок",
        "Playwright технически запрещает объединять несколько expect() в одном методе"
      ],
      "correct": 0,
      "explain": "Одна проверка отвечает на вопрос «мы на нужной странице?», другая — «отображается ли ожидаемое содержимое?»; разделение по смыслу упрощает чтение и диагностику."
    },
    {
      "question": "Сколько локаторов хранится в конструкторе DashboardPage в рамках сценария этого курса?",
      "options": [
        "Четыре, включая URL",
        "Три — по одному на каждый пункт меню",
        "Один — локатор заголовка toolbar_title",
        "Ни одного"
      ],
      "correct": 2,
      "explain": "DashboardPage используется в курсе только для финальной проверки, поэтому в конструкторе достаточно одного локатора — заголовка страницы."
    },
    {
      "question": "Что находится в атрибуте класса URL в DashboardPage?",
      "options": [
        "Имя фикстуры, создающей объект страницы",
        "Адрес страницы Dashboard",
        "Текст, который должен отображаться в заголовке",
        "Локатор заголовка страницы"
      ],
      "correct": 1,
      "explain": "URL — атрибут класса, хранящий адрес страницы Dashboard; он используется методом check_opened() для проверки перехода."
    }
  ]
}
