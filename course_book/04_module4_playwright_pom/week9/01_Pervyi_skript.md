# 4.6. Первый Playwright-скрипт: goto, locator, fill, click

## Зачем это нужно

Это первый урок, где ты напишешь реально работающий код на Playwright. Дальше в модуле мы будем постоянно опираться на эти четыре операции — `goto`, `locator`, `fill`, `click` — они встречаются в любом UI-автотесте, независимо от того, что он проверяет.

## Теория

Важное уточнение сразу: то, что мы напишем в этом уроке — ещё не тест. Тест не просто выполняет действия, а ещё и проверяет результат через `assert` или `expect()`. Пока проверок не будет, поэтому правильнее называть файл скриптом. Про `expect()` поговорим в следующем уроке (4.7), а полноценные `pytest`-тесты у нас уже есть в `boilerplate/tests/ui/test_registration.py` — на неделе 10 разберём его архитектуру.

### Запуск браузера и создание страницы

Любой Playwright-скрипт в синхронном режиме начинается с одной и той же конструкции:

```python
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch(headless=False)
    page = browser.new_page()

    browser.close()
```

Разберём по частям:

- `from playwright.sync_api import sync_playwright` — импортирует точку входа в Playwright.
- `with sync_playwright() as p:` — запускает Playwright и создаёт объект `p`, через который дальше запускается браузер.
- `browser = p.chromium.launch(headless=False)` — запускает браузер Chromium в видимом режиме.
- `page = browser.new_page()` — создаёт новую вкладку. `page` — один из главных объектов Playwright: через него выполняется открытие страниц, поиск элементов, ввод текста, клики, проверки.
- `browser.close()` — закрывает браузер после завершения работы.

Если запустить такой скрипт, браузер откроется, создаст пустую вкладку и почти сразу же закроется — между действиями нет пауз, и Python выполняет их за доли секунды.

У объекта `p` есть не только `.chromium`, но и `.firefox`, `.webkit` — три разных браузерных движка с одинаковым API (`.launch()`, `.new_page()` и так далее устроены одинаково для всех трёх). Отличается только то, какой реальный браузер запускается "под капотом". В курсе мы всегда используем `chromium` как самый распространённый вариант, но тот же самый скрипт, по сути, запустится и на `p.firefox.launch(...)` почти без изменений.

Параметр `headless` определяет, покажется ли браузер на экране: `headless=False` — открывается видимое окно (удобно, пока разбираешься, что происходит на странице), `headless=True` — браузер работает без видимого окна и быстрее. Именно `headless=True` обычно используют при запуске тестов в CI, где физического экрана нет вообще.

### `page.goto()` — открываем страницу

```python
page.goto("https://nikita-filonov.github.io/qa-automation-engineer-ui-course/#/auth/registration")
```

Метод `page.goto(url)` открывает страницу по указанному адресу. `page` — это объект, через который Playwright управляет открытой вкладкой браузера, и почти вся дальнейшая работа идёт именно через него: `page.goto(...)`, `page.locator(...)`, `page.url`.

Кстати, `page.goto(...)` не просто открывает страницу "в пустоту" — он возвращает объект ответа сервера на главный запрос страницы. Прямо сейчас он нам не нужен, но не удивляйся, если в чужом коде встретишь `response = page.goto(url)` с дальнейшей проверкой чего-то вроде `response.status`.

### `page.locator()` — находим элемент на странице

```python
page.locator("[data-testid='registration-form-email-input'] input")
```

`page.locator(...)` находит элемент на странице по локатору (мы разобрали эти локаторы на неделе 8). Важная деталь на будущее: сам по себе вызов `page.locator(...)` не ищет элемент немедленно и не падает с ошибкой, если элемента пока нет на странице, — он просто создаёт объект-локатор, описывающий, **где именно искать**. Реальный поиск (и, если нужно, ожидание) происходит только в момент, когда на этом локаторе вызывается действие — `.fill()`, `.click()` и так далее. Почему это важно — станет понятнее в следующем уроке, про автоматические ожидания.

### `.fill()` — вводим текст

```python
page.locator("[data-testid='registration-form-email-input'] input").fill("user@example.com")
```

`.fill(...)` вводит текст в найденное поле. Важно: `fill` не дописывает текст в конец поля, а сначала очищает его и затем устанавливает новое значение — это то, что обычно и нужно при заполнении формы.

### `.click()` — нажимаем кнопку

```python
page.locator("[data-testid='registration-page-registration-button']").click()
```

`.click()` выполняет клик по найденному элементу.

Ещё один момент, о котором полезно знать заранее: если локатор находит **несколько** элементов одновременно (например, слишком общий CSS-селектор совпал сразу с тремя полями формы), Playwright не станет кликать по первому попавшемуся и не кликнет по всем сразу — он остановится с ошибкой о том, что локатор указывает не на один элемент, а на несколько (это называется strict mode). Это осознанное поведение: неоднозначное действие — частый источник тестов, которые ломаются непредсказуемо, поэтому Playwright требует, чтобы локатор для действия указывал ровно на один элемент.

## Пример

Полный сценарий — от открытия страницы до нажатия кнопки Registration:

```python
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.launch(headless=False)
    page = browser.new_page()

    page.goto(
        "https://nikita-filonov.github.io/qa-automation-engineer-ui-course/#/auth/registration"
    )

    page.locator("[data-testid='registration-form-email-input'] input").fill("user@example.com")
    page.locator("[data-testid='registration-form-username-input'] input").fill("testuser")
    page.locator("[data-testid='registration-form-password-input'] input").fill("password123")
    page.locator("[data-testid='registration-page-registration-button']").click()

    browser.close()
```

Порядок действий важен: Playwright выполняет команды сверху вниз, и нельзя заполнить поле до того, как открылась страница, или нажать кнопку раньше, чем заполнены обязательные поля.

Чтобы увидеть результат своими глазами (иначе браузер закроется слишком быстро), временно добавь перед `browser.close()`:

```python
input("Нажмите Enter для завершения...")
```

После проверки эту строку нужно убрать — это временный инструмент отладки, а не часть сценария. Позже, когда убедишься, что скрипт работает правильно, эту же строку можно заменить на `headless=True` при запуске браузера — тогда визуальная проверка вообще не понадобится.

Сохрани скрипт как `registration_script.py` в корне своего форка `boilerplate/` (это отдельный экспериментальный файл, не путай с тестами в `tests/ui/`) и запусти:

```bash
python -m registration_script
```

## Частые ошибки

- Забыть, что `fill()` требует, чтобы элемент уже был на странице — если локатор написан с ошибкой, Playwright будет ждать появления элемента и в итоге упадёт с таймаутом.
- Перепутать порядок действий: попытаться нажать кнопку до заполнения полей.
- Не активировать виртуальное окружение или забыть выполнить `playwright install chromium` перед запуском — тогда Python либо не найдёт установленный `playwright`, либо браузер не запустится (в обоих случаях Playwright обычно сам подсказывает нужную команду в тексте ошибки).
- Использовать слишком общий локатор, который находит сразу несколько элементов вместо одного, — вместо клика или заполнения Playwright выбросит ошибку о неоднозначном совпадении (strict mode), а не выберет "первый попавшийся".

## Мини-задания

1. **Разминка.** Возьми скрипт из примера, но сократи его до одного действия: открой страницу регистрации и с помощью `page.locator(...)` и `.fill()` заполни только поле email своим адресом. Добавь `input("Нажмите Enter для завершения...")` перед `browser.close()` и убедись глазами, что заполнилось именно email, а остальные поля формы остались пустыми.

2. **Основное.** Возьми скрипт из примера и измени значения email, username и password на свои собственные. Добавь `input("Нажмите Enter для завершения...")` перед `browser.close()` и убедись, что все три поля действительно заполнились правильными значениями — глазами, в открывшемся окне браузера.

3. **Ближе к практике.** Сделай копию скрипта из примера, в которой браузер запускается с `headless=True` (без видимого окна). Убери `input(...)`, а вместо визуальной проверки добавь `print(page.url)` сразу после клика по кнопке Registration. Запусти скрипт и по адресу, выведенному в консоль, убедись, что переход на Dashboard произошёл, даже если ты ни разу не увидел браузер на экране.

4. **Со звёздочкой.** Специально вызови ошибку "неоднозначного локатора": на странице регистрации несколько тегов `<input>` (email, username, password), поэтому локатор `page.locator("input")` найдёт сразу три элемента. Попробуй вызвать на нём `.fill("test")` и посмотри на текст ошибки, которую выдаст Playwright. Затем перепиши локатор так, чтобы он однозначно указывал на одно конкретное поле, и убедись, что `.fill()` срабатывает без ошибок.
