{
  "questions": [
    {
      "question": "Какой метод Playwright используется для отметки чекбокса (<input type='checkbox'>)?",
      "options": [
        ".fill()",
        ".select_option()",
        ".check()",
        ".click(), потому что других способов не существует"
      ],
      "correct": 2,
      "explain": "Для чекбоксов и радиокнопок в Playwright есть отдельные методы .check()/.uncheck() — они устанавливают нужное состояние явно, а не просто переключают текущее, как это сделал бы обычный клик."
    },
    {
      "question": "Какой метод Playwright используется для выбора пункта в стандартном HTML-элементе <select>?",
      "options": [
        ".fill()",
        ".select_option()",
        ".check()",
        ".pick_option()"
      ],
      "correct": 1,
      "explain": "Для тега <select> используется отдельный метод select_option(), который может принимать value, видимый текст варианта или индекс."
    },
    {
      "question": "Почему .select_option() может не сработать для компонента, который визуально выглядит как выпадающий список?",
      "options": [
        "select_option() работает только в Firefox",
        "Если это кастомный компонент (набор div/li вместо тега select), в разметке физически нет тега select, и метод не найдёт нужный элемент",
        "select_option() требует предварительного вызова .click()",
        "select_option() устарел и заменён на .fill()"
      ],
      "correct": 1,
      "explain": "Если вместо стандартного <select> используется стилизованный компонент из div и li, select_option() не подойдёт — нужно кликать по элементам как по обычным DOM-узлам."
    },
    {
      "question": "Как проверить, что чекбокс действительно находится в отмеченном состоянии?",
      "options": [
        "expect(locator).to_be_checked()",
        "expect(locator).to_have_text(\"checked\")",
        "assert locator.value == True",
        "expect(locator).to_be_visible()"
      ],
      "correct": 0,
      "explain": "to_be_checked() — специальная проверка для чекбоксов и радиокнопок, подтверждающая, что элемент находится в отмеченном состоянии."
    },
    {
      "question": "Чем .check() отличается от обычного .click() применительно к чекбоксу?",
      "options": [
        "Разницы нет, это синонимы",
        ".check() всегда снимает отметку, а .click() устанавливает",
        ".check() устанавливает отмеченное состояние, если оно ещё не установлено, тогда как .click() просто переключает текущее состояние",
        ".check() работает только с радиокнопками, а не с чекбоксами"
      ],
      "correct": 2,
      "explain": ".check() идемпотентен: если чекбокс уже отмечен, повторный вызов ничего не изменит, тогда как .click() всегда переключает состояние на противоположное."
    }
  ]
}
