{
  "question": "Почему BaseUrlSession в общем репозитории переопределяет именно метод request(), а не отдельно get(), post() и т.д.?",
  "options": [
    "Потому что requests.Session не позволяет переопределять метод get() вообще",
    "Потому что метод request() — единственный метод, который умеет работать с JSON",
    "Потому что requests.Session вызывает request() внутри любого метода (get/post/put/...), и переопределение request() гарантирует подстановку base_url для всех методов сразу",
    "Потому что переопределение request() ускоряет выполнение HTTP-запросов"
  ],
  "correct": 2,
  "explain": "session.get(...), session.post(...) и остальные методы внутри себя вызывают session.request(...). Переопределив только request(), мы получаем подстановку base_url сразу для всех HTTP-методов."
}
