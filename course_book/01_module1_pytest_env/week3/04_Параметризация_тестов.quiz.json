{
  "questions": [
    {
      "question": "В коде `@pytest.mark.parametrize(\"text, expected\", [(\"racecar\", True, \"extra\")])` кортеж содержит три значения, а имён аргументов указано только два. Что произойдёт при запуске?",
      "options": [
        "pytest проигнорирует лишнее значение, и тест выполнится с двумя первыми значениями",
        "Тест выполнится, а третье значение станет доступно как отдельный параметр extra",
        "pytest упадёт с ошибкой ещё на этапе сбора тестов, до их выполнения",
        "Будет создано два отдельных теста: один с text и expected, второй с extra"
      ],
      "correct": 2,
      "explain": "Количество значений в каждом наборе parametrize должно совпадать с количеством имён аргументов. Несовпадение — это ошибка конфигурации теста, и pytest сообщает о ней сразу при сборе тестов, а не пытается угадать, куда девать лишнее значение."
    },
    {
      "question": "Тест декорирован `@pytest.mark.parametrize(\"text, expected\", [(\"racecar\", True), (\"hello\", False), (\"\", True)])`. Сколько отдельных прогонов теста создаст pytest?",
      "options": [
        "Один — все данные передаются в один общий вызов теста",
        "Три — по одному прогону на каждый кортеж значений в списке",
        "Два — pytest объединяет прогоны с одинаковым типом ожидаемого результата",
        "Шесть — по числу всех значений во всех кортежах суммарно"
      ],
      "correct": 1,
      "explain": "Каждый кортеж из списка parametrize превращается в отдельный, независимый прогон теста — здесь три кортежа, значит будет три прогона со своими id в выводе."
    },
    {
      "question": "Зачем в @pytest.mark.parametrize нужен необязательный параметр ids?",
      "options": [
        "Без него pytest вообще откажется запускать параметризованный тест",
        "Он задаёт понятные, читаемые идентификаторы для каждого набора данных вместо автоматически сгенерированных",
        "Он определяет порядок, в котором должны выполняться параметризованные прогоны",
        "Он подставляет случайные значения id для защиты от повторного запуска одних и тех же тестов"
      ],
      "correct": 1,
      "explain": "Когда автоматические id получаются длинными или нечитаемыми (например, для сложных объектов), ids позволяет вручную задать понятные подписи для каждого набора параметров в выводе pytest."
    },
    {
      "question": "Что произойдёт, если применить @pytest.mark.parametrize дважды к одной тестовой функции с разными наборами данных?",
      "options": [
        "Сработает только последний по порядку декоратор, первый будет проигнорирован",
        "pytest выполнит тест для всех комбинаций значений из обоих декораторов (декартово произведение)",
        "pytest выдаст ошибку — двойная параметризация запрещена",
        "Значения из обоих декораторов объединятся в один общий плоский список"
      ],
      "correct": 1,
      "explain": "Два декоратора parametrize на одной функции комбинируются как декартово произведение — pytest выполнит тест для каждой пары значений из первого и второго набора."
    },
    {
      "question": "В проекте есть 5 однотипных тестовых функций, отличающихся только входным значением и ожидаемым результатом при одинаковой логике проверки. Что стоит сделать по практике этого урока?",
      "options": [
        "Оставить как есть — параметризация оправдана только начиная с 10+ наборов данных",
        "Объединить их в один тест с @pytest.mark.parametrize, передав все 5 наборов данных",
        "Удалить 4 из них, оставив только один «репрезентативный» тест",
        "Переписать логику через pytest.raises, чтобы сократить количество строк кода"
      ],
      "correct": 1,
      "explain": "Если тело теста и логика проверки одинаковы, а различаются только данные — это прямой сигнал использовать parametrize вместо копирования почти идентичных функций."
    }
  ]
}
