{
  "exercises": [
    {
      "prompt": "Дан словарь config = {\"base_url\": \"https://api.example.com\", \"timeout\": 30}. Через .get() сохрани в timeout значение по ключу \"timeout\", а в retries — значение по ключу \"retries\" со значением по умолчанию 3 (такого ключа нет).",
      "starter": "config = {\"base_url\": \"https://api.example.com\", \"timeout\": 30}\n\n# TODO: значение по ключу \"timeout\" через .get()\ntimeout = None\n\n# TODO: значение по ключу \"retries\" через .get() со значением по умолчанию 3\nretries = None\n",
      "tests": "def test_timeout_value():\n    assert timeout == 30, f\"ожидалось 30, получено {timeout}\"\n\n\ndef test_retries_default():\n    assert retries == 3, f\"ключа 'retries' нет в config, .get() должен вернуть значение по умолчанию 3, получено {retries}\"\n"
    },
    {
      "prompt": "Дан кортеж point = (10, 20). Распакуй его в переменные x и y. Дан список с повторами visited_pages = [\"home\", \"cart\", \"home\", \"checkout\", \"cart\"] — преврати его в множество unique_pages без дубликатов и сохрани в pages_count количество уникальных страниц.",
      "starter": "point = (10, 20)\n\n# TODO: распакуй point в x и y\nx = None\ny = None\n\nvisited_pages = [\"home\", \"cart\", \"home\", \"checkout\", \"cart\"]\n\n# TODO: множество уникальных страниц\nunique_pages = None\n# TODO: количество уникальных страниц\npages_count = None\n",
      "tests": "def test_point_unpacked():\n    assert x == 10 and y == 20, f\"ожидалось x=10, y=20, получено x={x}, y={y}\"\n\n\ndef test_unique_pages():\n    assert unique_pages == {\"home\", \"cart\", \"checkout\"}, f\"получено {unique_pages}\"\n\n\ndef test_pages_count():\n    assert pages_count == 3, f\"ожидалось 3 уникальные страницы, получено {pages_count}\"\n"
    },
    {
      "prompt": "run_1_failed = {\"test_login\", \"test_cart\", \"test_search\"}, run_2_failed = {\"test_cart\", \"test_checkout\"}. Сохрани в still_failing тесты, упавшие в обоих прогонах (пересечение), а в fixed_since_run_1 — тесты, упавшие в первом прогоне, но не во втором.",
      "starter": "run_1_failed = {\"test_login\", \"test_cart\", \"test_search\"}\nrun_2_failed = {\"test_cart\", \"test_checkout\"}\n\n# TODO: пересечение — тесты, упавшие в обоих прогонах\nstill_failing = None\n\n# TODO: разность — тесты из run_1_failed, которых нет в run_2_failed\nfixed_since_run_1 = None\n",
      "tests": "def test_still_failing():\n    assert still_failing == {\"test_cart\"}, f\"ожидалось {{'test_cart'}}, получено {still_failing}\"\n\n\ndef test_fixed_since_run_1():\n    assert fixed_since_run_1 == {\"test_login\", \"test_search\"}, f\"получено {fixed_since_run_1}\"\n"
    },
    {
      "prompt": "Заведи empty_set = set() и empty_dict = {} — через type(x).__name__ сохрани их имена типов в empty_set_type и empty_dict_type. Дан settings = {\"retries\": 0}. Сохрани в value_with_default результат settings.get(\"retries\", 5), а в value_if_missing — результат {}.get(\"retries\", 5) (пустой словарь, ключа нет вовсе).",
      "starter": "empty_set = set()\nempty_dict = {}\n\n# TODO: имя типа empty_set через type(...).__name__\nempty_set_type = None\n# TODO: имя типа empty_dict через type(...).__name__\nempty_dict_type = None\n\nsettings = {\"retries\": 0}\n\n# TODO: settings.get(\"retries\", 5) — ключ есть, но значение 0\nvalue_with_default = None\n\n# TODO: {}.get(\"retries\", 5) — ключа нет вовсе\nvalue_if_missing = None\n",
      "tests": "def test_empty_set_type():\n    assert empty_set_type == \"set\", f\"set() должен иметь тип 'set', получено {empty_set_type}\"\n\n\ndef test_empty_dict_type():\n    assert empty_dict_type == \"dict\", f\"{{}} создаёт словарь, а не множество, ожидалось 'dict', получено {empty_dict_type}\"\n\n\ndef test_value_with_default_is_zero():\n    assert value_with_default == 0, \"ключ 'retries' есть и равен 0 — .get() должен вернуть именно 0, а не значение по умолчанию 5\"\n\n\ndef test_value_if_missing_is_default():\n    assert value_if_missing == 5, \"в пустом словаре ключа 'retries' нет вовсе — .get() должен вернуть значение по умолчанию 5\"\n"
    }
  ]
}
