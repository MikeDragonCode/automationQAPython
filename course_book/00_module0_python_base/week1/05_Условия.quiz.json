{
  "question": "Что будет выведено в результате выполнения кода?\nx = 5\nif x < 0:\n    x = -x\n    print(x)\nprint(\"done\")",
  "options": [
    "5\ndone",
    "-5\ndone",
    "done",
    "Ошибка выполнения"
  ],
  "correct": 2,
  "explain": "print(x) находится внутри блока if (определяется отступом) и выполнится только при x < 0. Здесь x = 5, условие ложно, поэтому выведется только 'done'."
}
