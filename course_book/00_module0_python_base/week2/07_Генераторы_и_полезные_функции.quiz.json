{
  "questions": [
    {
      "question": "Что нужно сделать, чтобы получить обычный список из результата map(int, ['1', '2', '3'])?",
      "options": [
        "Обернуть результат в list(): list(map(int, ['1', '2', '3']))",
        "Вызвать .to_list() у результата map()",
        "Ничего не нужно — map() уже возвращает список",
        "Использовать map вместо filter"
      ],
      "correct": 0,
      "explain": "map() возвращает итератор, а не готовый список — его нужно явно обернуть в list(), если требуется сохранить или несколько раз использовать результат."
    },
    {
      "question": "Есть список словарей users = [{'name': 'Ivan', 'age': 25}, {'name': 'Olga', 'age': 19}]. Что произойдёт при вызове sorted(users) без параметра key?",
      "options": [
        "Возникнет ошибка — Python не знает, как сравнивать два словаря между собой без key",
        "Функция вернёт список в исходном порядке без изменений",
        "Список отсортируется по полю 'name' автоматически",
        "Список отсортируется по полю 'age' автоматически"
      ],
      "correct": 0,
      "explain": "Словари сами по себе не поддерживают сравнение больше/меньше. Параметр key явно указывает, какое значение из каждого элемента использовать для сравнения при сортировке."
    },
    {
      "question": "Что делает filter(lambda x: x % 2 == 0, numbers)?",
      "options": [
        "Удаляет из numbers все чётные элементы",
        "Оставляет только те элементы numbers, для которых lambda вернула True",
        "Сортирует numbers по чётности элементов",
        "Применяет lambda к каждому элементу и возвращает список результатов вызова"
      ],
      "correct": 1,
      "explain": "filter() проверяет каждый элемент функцией-предикатом и оставляет только те, для которых она вернула True — в данном случае это чётные числа."
    },
    {
      "question": "Дано names = ['ivan', 'olga', 'petr'] и ages = [25, 30]. Сколько пар вернёт zip(names, ages)?",
      "options": [
        "0 пар — списки разной длины нельзя передавать в zip",
        "2 пары — zip останавливается на самой короткой коллекции",
        "Ошибка выполнения",
        "3 пары, для третьего имени age будет None"
      ],
      "correct": 1,
      "explain": "zip() перебирает коллекции параллельно и останавливается, как только заканчивается самая короткая из них — недостающих значений он не придумывает и не выбрасывает ошибку."
    },
    {
      "question": "Дан список statuses = [200, 500, 200]. Что вернёт any(s >= 500 for s in statuses)?",
      "options": [
        "False — не все элементы удовлетворяют условию",
        "Список из булевых значений [False, True, False]",
        "True — хотя бы один элемент удовлетворяет условию",
        "500 — само значение, которое подошло"
      ],
      "correct": 2,
      "explain": "any() возвращает True, если истинен хотя бы один элемент. Здесь второй статус (500) удовлетворяет условию s >= 500, этого достаточно для True."
    }
  ]
}
