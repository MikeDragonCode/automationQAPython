[pytest]
markers =
    ui: UI-тесты (Playwright)
    api: API-тесты (requests)
    unit: тесты чистых функций/утилит, без сети и без браузера
    smoke: быстрый набор критичных проверок
    regress: полный регрессионный набор

addopts = -ra --strict-markers
testpaths = tests
